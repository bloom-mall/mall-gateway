name: DEV

on:
  push:
    branches-ignore:
      - main

env:
  IMAGE_TAG: "lishuaidev/${{ github.event.repository.name }}:${{ github.sha }}.dev"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Style Check And Test
        run: ./gradlew check --info

      - name: Build Jar
        run: ./gradlew clean bootJar --no-daemon --parallel

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            build/
            Dockerfile


  manual-trigger-push:
    runs-on: ubuntu-latest
    outputs:
      CONTINUE_JOBS: ${{ steps.manual-trigger.outputs.CONTINUE_JOBS }}
    steps:
      - name: manual-trigger
        id: manual-trigger
        uses: ShuaiLLi/manual-trigger@v1.0.0
        with:
          RUN_NAME: "push"


  push:
    runs-on: ubuntu-latest
    needs: manual-trigger-push
    if: ${{ needs.manual-trigger-push.outputs.CONTINUE_JOBS == 'true' }}
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.IMAGE_TAG }}
